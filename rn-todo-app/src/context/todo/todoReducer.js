import {ADD_TODO, REMOVE_TODO, UPDATE_TODO} from './ types'

export const todoReducer = (state, action) => {
    switch (action.type) {
        case ADD_TODO:
            return state
    }
    return state
}